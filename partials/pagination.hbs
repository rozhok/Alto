<nav class="pagination container large">
    <div class="pagination-left">
        {{#if prev}}
            <a class="newer-posts button-arrow button-arrow-left" href="{{page_url prev}}">
                <i class="button-arrow-icon icon icon-arrow-left"></i> Обратно
            </a>
        {{/if}}
    </div>
    <span class="page-number hidden-xs hidden-sm">Страница {{page}} из {{pages}}</span>
    <div class="pagination-right">
        {{#if next}}
            <a class="older-posts button-arrow button-arrow-right" href="{{page_url next}}">
                Туда <i class="button-arrow-icon icon icon-arrow-right"></i>
            </a>
        {{/if}}
    </div>
</nav>